name: "my_mcp"
description: A Model Context Protocol (MCP) server.
version: 1.0.3
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.7.0
  html: ^0.15.6
  http: ^1.6.0
  logging: ^1.3.0
  mcp_dart: ^2.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.31.0
